A machine learning project for training different models to classify news as Positive, Negative, or Neutral. Includes preprocessing, model training, and evaluation.

## 🚀 Features
- Text cleaning & preprocessing
- TF-IDF or embeddings for vectorization
- Multiple ML models
- Evaluation metrics: accuracy, precision, recall, F1
